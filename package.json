{
    "name": "@ioto/monorepo",
    "private": true,
    "scripts": {
        "publish": "lerna publish --no-git-checks",
        "build:all": "lerna exec pnpm build"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^5.6.2"
    }
}
